# ==========================================================
# A.C.E. Project Dependencies
# Use 'pip3 install -r requirements.txt' to install all.
# ==========================================================

# Core dependencies (always required)
# For making HTTP requests to APIs (GitHub, News)
requests>=2.25.0

# For securely loading credentials from the .env file
python-dotenv>=0.19.0

# For parsing RSS feeds in the News Hub feature
feedparser>=6.0.0

# For the Task Scheduler feature
schedule>=1.1.0

# ==========================================================
# Voice Interface Dependencies (Optional)
# These require system audio libraries to be installed first.
# See README.md for system audio setup instructions.
# Uncomment the lines below if you want voice features:
# ==========================================================

# For converting speech-to-text in the voice interface
# SpeechRecognition>=3.8.0

# For converting text-to-speech (works offline)
# pyttsx3>=2.90

# For accessing the microphone (required by SpeechRecognition)
# Note: This package often requires system audio libraries
# PyAudio>=0.2.11

# ==========================================================
# Installation Instructions:
# 
# Core installation (recommended):
#   pip3 install -r requirements.txt
# 
# With voice features (requires audio system setup):
#   1. Install system audio libraries (see README.md)
#   2. Uncomment voice dependencies above
#   3. pip3 install -r requirements.txt
# 
# Core dependencies only (if audio installation fails):
#   pip3 install requests python-dotenv feedparser schedule
# ==========================================================
textual
